[project]
name = "tiny-transformer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "torch>=2.4",
    "torchvision>=0.19",
    "Pillow>=10",
    "pytest>=8",
]
